import { type IncomingSyncStream, type OutgoingSyncQueue, type Peer } from "cojson";
import type { SQLiteDatabaseDriver } from "./types.js";
export declare class SQLiteNodeBase {
    private readonly syncManager;
    private readonly dbClient;
    constructor(db: SQLiteDatabaseDriver, fromLocalNode: IncomingSyncStream, toLocalNode: OutgoingSyncQueue, maxBlockingTime: number);
    static create({ db, localNodeName, maxBlockingTime, }: {
        db: SQLiteDatabaseDriver;
        localNodeName?: string;
        maxBlockingTime?: number;
    }): Peer;
}
//# sourceMappingURL=node.d.ts.map