import { type IncomingSyncStream, type OutgoingSyncQueue, type Peer } from "cojson";
import type { SQLiteDatabaseDriverAsync } from "./types.js";
export declare class SQLiteNodeBaseAsync {
    private readonly syncManager;
    private readonly dbClient;
    constructor(db: SQLiteDatabaseDriverAsync, fromLocalNode: IncomingSyncStream, toLocalNode: OutgoingSyncQueue);
    static create({ db, localNodeName, }: {
        db: SQLiteDatabaseDriverAsync;
        localNodeName?: string;
    }): Promise<Peer>;
}
//# sourceMappingURL=node.d.ts.map